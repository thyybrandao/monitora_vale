import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:covidvale/app/modules/home/pages/decretos/decretos_page.dart';

main() {
  testWidgets('DecretosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DecretosPage(title: 'Decretos')));
    //  final titleFinder = find.text('Decretos');
    //  expect(titleFinder, findsOneWidget);
  });
}
