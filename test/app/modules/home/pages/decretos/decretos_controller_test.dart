import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:covidvale/app/modules/home/pages/decretos/decretos_controller.dart';
import 'package:covidvale/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // DecretosController decretos;
  //
  setUp(() {
    //     decretos = HomeModule.to.get<DecretosController>();
  });

  group('DecretosController Test', () {
    //   test("First Test", () {
    //     expect(decretos, isInstanceOf<DecretosController>());
    //   });

    //   test("Set Value", () {
    //     expect(decretos.value, equals(0));
    //     decretos.increment();
    //     expect(decretos.value, equals(1));
    //   });
  });
}
