import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:covidvale/app/modules/decretos/decretos_module.dart';

void main() {
  initModule(DecretosModule());
  // DecretosController decretos;
  //
  setUp(() {
    //     decretos = DecretosModule.to.get<DecretosController>();
  });

  group('DecretosController Test', () {
    //   test("First Test", () {
    //     expect(decretos, isInstanceOf<DecretosController>());
    //   });

    //   test("Set Value", () {
    //     expect(decretos.value, equals(0));
    //     decretos.increment();
    //     expect(decretos.value, equals(1));
    //   });
  });
}
