import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:covidvale/app/modules/telefones/telefones_page.dart';

main() {
  testWidgets('TelefonesPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TelefonesPage(title: 'Telefones')));
    //  final titleFinder = find.text('Telefones');
    //  expect(titleFinder, findsOneWidget);
  });
}
