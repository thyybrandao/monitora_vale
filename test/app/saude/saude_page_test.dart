import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:covidvale/app/saude/saude_page.dart';

main() {
  testWidgets('SaudePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SaudePage(title: 'Saude')));
    //  final titleFinder = find.text('Saude');
    //  expect(titleFinder, findsOneWidget);
  });
}
